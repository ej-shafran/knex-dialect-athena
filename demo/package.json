{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "demo",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@aws-sdk/client-athena": "^3.679.0",
    "dotenv": "^16.4.5",
    "knex": "^3.1.0",
    "knex-dialect-athena": "workspace:*"
  }
}
